# Frontend web developer background

The user builds frontend web apps today. They can lean on JavaScript/TypeScript and React-shaped mental models. Map mobile concepts to web equivalents (localStorage → AsyncStorage, fetch to API → local SQLite queries, download link → native share sheet) rather than teaching programming from scratch.
